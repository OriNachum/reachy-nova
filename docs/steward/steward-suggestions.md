# Steward suggestions

> Auto-generated by `steward doctor` on 2026-06-13.
> Re-run `steward doctor --scope siblings` to refresh.
> See `docs/perfect-patient.md` in the steward checkout for the corpus baseline.

Repo: `reachy_nova` — 1 agent(s) declared.

## Repo-level findings

- **portability** `.`: ❌ Hard-coded /home/<user>/ paths:
    docs/configuration/openwakeword.md:109:WAKE_WORD_MODEL=/home/spark/models/parakeet-finetuned.nemo
   Fix: use ../sibling, repo URL, or $WORKSPACE/sibling instead.

❌ Per-user ~/.<dotfile> config refs in committed doc/config:
    docs/components/continous_experience.md:22:  -> SessionState.load()                  # read ~/.reachy_nova/session/session.json
    docs/components/continous_experience.md:66:**Path:** `~/.reachy_nova/session/session.json`
    docs/components/continous_experience.md:68:Follows the same `~/.reachy_nova/` convention as [Face Recognition](face_recognition.md) (`~/.reachy_nova/faces/`).
    docs/components/face_recognition.md:77:    -   **Metadata**: `~/.reachy_nova/faces/faces.json`
    docs/components/face_recognition.md:78:    -   **Embeddings**: `~/.reachy_nova/faces/embeddings/<id>.npy`
    docs/configuration/openwakeword.md:142:- First run downloads ~1.2 GB. Subsequent runs use NeMo's local cache (usually `~/.cache/huggingface/`).
   Allowed carve-outs: ~/.claude/skills/.../scripts/ (tool calls), ~/.culture/ (mesh data).
   Otherwise: commit a repo-local config or document a portable lookup.

## Agent: `nova`

- Backend: `claude`

- **warning** (agent-vs-baseline): missing baseline skill `cicd`
- **warning** (agent-vs-baseline): missing baseline skill `communicate`
- **warning** (agent-vs-baseline): missing baseline skill `run-tests`
- **warning** (agent-vs-baseline): missing baseline skill `sonarclaude`
- **warning** (agent-vs-baseline): missing baseline skill `version-bump`
- **info** (agent-vs-baseline): missing recommended skill `agent-config` (30–80% of corpus or promoted)
- **info** (agent-vs-baseline): missing recommended skill `ask-colleague` (30–80% of corpus or promoted)
- **info** (agent-vs-baseline): missing recommended skill `assign-to-workforce` (30–80% of corpus or promoted)
- **info** (agent-vs-baseline): missing recommended skill `doc-test-alignment` (30–80% of corpus or promoted)
- **info** (agent-vs-baseline): missing recommended skill `pypi-maintainer` (30–80% of corpus or promoted)
- **info** (agent-vs-baseline): missing recommended skill `spec-to-plan` (30–80% of corpus or promoted)
- **info** (agent-vs-baseline): missing recommended skill `think` (30–80% of corpus or promoted)
