"""Placeholder test proving the pytest scaffold works on a fresh checkout.

This intentionally does not import anything from ``reachy_nova`` yet - it
only proves that ``uv run pytest`` collects and runs the suite green
without needing the hardware/cloud-heavy runtime dependencies installed.
Real coverage lands with the tasks that follow this scaffold.
"""


def test_placeholder() -> None:
    assert True
